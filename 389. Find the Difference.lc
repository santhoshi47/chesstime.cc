class Solution {
    public char findTheDifference(String s, String t) {
        char c=0;
        for(char a:s.toCharArray())
            c^=a;
        for(char b:t.toCharArray())
            c^=b;
    return c;
}}


we use xor to find common elements..x^x=0,,,,,x^0=x so common elements become 0,others are displayed.
